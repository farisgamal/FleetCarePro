@model decimal

<div class="card mb-4">
    <div class="card-header bg-success text-white">Total Maintenance Cost (Current Month)</div>
    <div class="card-body">
        <h3>$@Model.ToString("N2")</h3>
    </div>
</div>
